# Template for RepoDir
Alias /parcels/ "/repository/REP_WEB_SERVER_DIR_REP/"

<Directory "/repository/REP_WEB_SERVER_DIR_REP/">
        Options All Indexes
        AllowOverride all
        Order allow,deny
        Allow from all
    </Directory>
<DirectoryMatch  /repository/REP_WEB_SERVER_DIR_REP/(.)*/>
        Options +Indexes
</DirectoryMatch>

# ServerName templateMasterNode:80

